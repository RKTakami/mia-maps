# Build output
.gradle/
build/
bin/
out/
map-native/target/

# Minecraft run directory (world data, logs — may contain personal info)
run/

# IDEs / editors
.idea/
.vscode/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/
.factorypath

# Vendored / local dependencies (not distributed)
libs/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Logs, environment, and local overrides (may contain personal info)
*.log
logs/
.env
*.env
*.local
*.hprof

# Working notes: kept on disk, out of the public repo. Still present in git HISTORY —
# untracking does not retroactively remove them from commits already pushed.
AGENTS.md
project_memory.md
